{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: build HandmadeHero",
            "type": "shell",
            "command": "cmake --build Build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}